{
  "name": "bespoke-edge-metrics",
  "version": "0.1.0",
  "bin": {
    "bespoke-edge-metrics": "bin/bespoke-edge-metrics.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk"
  },
  "devDependencies": {
    "aws-cdk": "2.160.0",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.160.0",
    "constructs": "^10.0.0"
  }
}
